import { MantineNumberSize } from '@mantine/core';

interface Settings {
  menuBreak: MantineNumberSize;
  headerHeight: number;
}
export const settings: Settings = {
  menuBreak: 'sm',
  headerHeight: 60,
};
